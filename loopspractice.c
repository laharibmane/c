#include <stdio.h>

int main()
{
    // Iterate through the range and print odd numbers
    for (int num = 19; num <= 190; num += 19)
    {
        printf("%d\n", num);
    }

    return 0; 
      
}
