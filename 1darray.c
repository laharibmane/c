#include <stdio.h>
/// @brief
/// @return
int main()
{
    int arr[5]; //  to store elements in an array and print them//
    int i;

    // Reading elements into the array
    for (i = 0; i < 5; i++)
    {
        printf("Enter element %d\n: ", i); // Prompt the user for input
        scanf("%d", &arr[i]);              // Read the input and store it in the array
    }

    // Print the array to verify the input
    printf("Array elements are:\n");
    for (i = 0; i < 5; i++)
    {
        printf("%d ", arr[i]);
    }
    printf("\n");

    return 0;
}
int main(){
    int i;
    int arr[20];
    //entering the number of elements into the array //
    printf("enter the number of elemnts : ");
    scanf("%d",&n);
    //prompt the user to enter the choice of elements 
    for(i=0;i<n;i++){
    printf("enter element %d\n:");
    scanf("%d",&arr[i]);
    }
    // print rge elements into the array 
    for(i=0;i<n;i++){
        printf("%d",arr[i]);
    }
    return 0;

    }

    // 

    int main(){
        int n;

        int arr[20];
        // to enter the number of elemnts 
        printf("enter the number of elements : ");
        scanf("%d",&n);
        // now enter the elemnts 
        for(i=0;i<0;i++){
            printf("enter the numbers :\n");
            scanf("%d",&arr[i]);
        }
        // now to print the all the numbers into the array
        for(i=0;i<n;i++){
            printf("%d")
        }
        }
        )
    }
