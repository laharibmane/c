#include <stdio.h>
int main()
{
    int x = 5;
    int y = 2;
    int z = x / y;
    printf("%d", z);
    return 0; //the output will be only 2 ,//
    //because we have used int data type instead of float // 
    
}