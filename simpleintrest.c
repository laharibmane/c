#include <stdio.h>
int main()
{
    float p = 100;// u can also write , float =p,r,t,si;
    float r = 10;
    float t = 2;
    float si = (p * r * t) / 100;
    printf("%f", si);
    return 0;
}
